## Managed by Ansible — do not edit manually
check filesystem {{ item.name }} with path {{ item.path }}
  if space usage > {{ item.warn | default(monit_disk_warn) }}% for 5 cycles then alert
  if space usage > {{ item.crit | default(monit_disk_crit) }}% for 5 cycles then alert
  if inode usage > {{ item.inode_warn | default(monit_inode_warn) }}% for 5 cycles then alert
  if inode usage > {{ item.inode_crit | default(monit_inode_crit) }}% for 5 cycles then alert
